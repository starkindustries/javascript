{
  "name": "babbling",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",    
    "build": "babel src -d js",
    "clean": "rm -rf js",
    "start": "node js/index.js",
    "watch": "watch 'npm run clean && npm run build && npm run start' ./src --interval=1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "watch": "^1.0.2"
  },
  "dependencies": {}
}
